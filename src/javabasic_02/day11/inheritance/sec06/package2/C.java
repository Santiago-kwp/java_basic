package javabasic_02.day11.inheritance.sec06.package2;


import javabasic_02.day11.inheritance.sec06.package1.A;

public class C extends A {
	//메소드 선언
    public void method(){
//        A a = new A();     // new 연산자를  통해 public으로 정의된 A의 생성자를 통한 인스턴스 생성은 가능
//        a.field = "value"; // 일반적인 인스턴스 접근이므로 protected로 제한된 필드에 접근 불가
//        a.method();       // 일반적인 인스턴스 접근이므로 protected로 제한된 메소드에는 접근 가능
    }

}